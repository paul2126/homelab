apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - config-pvc.yaml
# media-pv.yaml and media-pvc.yaml are excluded until the NFS export is created on TrueNAS
# (server: 192.168.1.21). Re-add them here and restore the media volume in deployment.yaml.
